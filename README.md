Password Manager
![Password Manager Preview](./assets/preview.png)
A modern and fully responsive **Password Manager** web application built using **React** and **Tailwind CSS**. This application allows users to save, view, and manage their website credentials through a clean and user-friendly interface.

The project focuses on strengthening core React concepts such as **state management**, **side effects**, handling user input, and working with browser storage.

Features

* ➕ Add and save website credentials
* 🔐 Store website name, username, and password
* 👁️ Show and hide passwords
* 📋 Copy credentials easily
* 🗑️ Delete saved passwords
* 💾 Passwords persist using **Local Storage**
* ⚡ Dynamic UI updates using React State
* 📱 Fully responsive design for mobile, tablet, and desktop
* 🎨 Clean and modern UI built with Tailwind CSS

## 🛠️ Technologies Used

* **React**
* **Tailwind CSS**
* **JavaScript**
* **React Hooks**

  * `useState`
  * `useEffect`
* **Local Storage**
* **React Icons**
* **UUID**

## Concepts Used

### State Management

The project uses the `useState` hook to manage form inputs, saved passwords, and UI states dynamically.

### useEffect Hook

The `useEffect` hook is used to handle side effects and load or manage saved password data when the application runs.

### Local Storage

Browser Local Storage is used to persist password data, allowing saved credentials to remain available even after refreshing the page.

### Responsive Design

The application is fully responsive and optimized for different screen sizes using Tailwind CSS utility classes.

## Quick Start

Follow these steps to run the project locally.

### 1. Clone the repository

```bash
git clone https://github.com/Bebsiizcool/PasswordManager-React
```

### 2. Navigate to the project directory

```bash
cd password-manager
```

### 3. Install dependencies

```bash
npm install
```

### 4. Install additional packages

```bash
npm install react-icons uuid
```

### 5. Start the development server

```bash
npm run dev
```

Open the local URL displayed in your terminal to view the application in your browser.

##  What I Learned

Through this project, I improved my understanding of:

* React component-based architecture
* Managing application state with `useState`
* Using `useEffect` for side effects
* Handling forms and user input
* Mapping and displaying dynamic data
* Adding, deleting, and managing items in state
* Using Local Storage to persist data
* Creating responsive layouts with Tailwind CSS

##  Responsive Design

The Password Manager is designed to work smoothly across:

*  Desktop
*  Mobile
*  Tablet
